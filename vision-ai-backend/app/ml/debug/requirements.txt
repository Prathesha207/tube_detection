# ---- duck_analyzer core ----
# torch: install the CUDA build FIRST (device: 0), e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
ultralytics>=8.3        # YOLO detector, bytetrack tracker, yolo11n.pt bottle model
lap>=0.5.12             # needed by ultralytics' bytetrack.yaml tracker
opencv-python>=4.8
numpy>=1.24
PyYAML>=6.0
mediapipe>=0.10         # hand_backend: mediapipe (code handles legacy + Tasks API)

# ---- optional but recommended ----
scipy>=1.10             # fast Hungarian matching; code falls back to its own solver without it

# ---- only if use_embedder: true (DeepEmbedder in embedder.py) ----
# torchvision>=0.16     # resnet backend
# transformers>=4.40    # siglip backend
